version: '3.8'

services:
  holiday-app:
    image: holiday-app:latest
    container_name: holiday-container
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
